CREATE TABLE restaurant (
                            id BIGINT AUTO_INCREMENT PRIMARY KEY,
                            name VARCHAR(255),
                            category VARCHAR(255),
                            lat DOUBLE,
                            lng DOUBLE
);
